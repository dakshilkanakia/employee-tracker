import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF4F46E5);
  static const primaryDark = Color(0xFF3730A3);
  static const primaryLight = Color(0xFF818CF8);
  static const primarySurface = Color(0xFFEEF2FF);

  // Sidebar
  static const sidebarBg = Color(0xFF1E1B4B);
  static const sidebarActive = Color(0xFF4F46E5);
  static const sidebarText = Color(0xFFBFB8F6);
  static const sidebarTextActive = Colors.white;

  // Neutrals
  static const background = Color(0xFFF8FAFC);
  static const surface = Colors.white;
  static const border = Color(0xFFE2E8F0);
  static const divider = Color(0xFFF1F5F9);

  // Text
  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);
  static const textMuted = Color(0xFF94A3B8);

  // Priority
  static const priorityHigh = Color(0xFFEF4444);
  static const priorityHighSurface = Color(0xFFFEF2F2);
  static const priorityMedium = Color(0xFFF59E0B);
  static const priorityMediumSurface = Color(0xFFFFFBEB);
  static const priorityLow = Color(0xFF10B981);
  static const priorityLowSurface = Color(0xFFECFDF5);

  // Status
  static const statusPending = Color(0xFF94A3B8);
  static const statusPendingSurface = Color(0xFFF8FAFC);
  static const statusInProgress = Color(0xFFF59E0B);
  static const statusInProgressSurface = Color(0xFFFFFBEB);
  static const statusDone = Color(0xFF10B981);
  static const statusDoneSurface = Color(0xFFECFDF5);

  // Semantic
  static const error = Color(0xFFEF4444);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);

  // Task palette (for color picker)
  static const List<Color> taskColors = [
    Color(0xFF4F46E5),
    Color(0xFF7C3AED),
    Color(0xFFDB2777),
    Color(0xFFEF4444),
    Color(0xFFF97316),
    Color(0xFFF59E0B),
    Color(0xFF10B981),
    Color(0xFF0EA5E9),
    Color(0xFF64748B),
  ];
}
